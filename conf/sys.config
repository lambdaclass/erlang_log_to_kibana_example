[
 %% We need UTC for logging.
 {sasl, [{utc_log, true}]},
 %% Lager configuration
 {lager,
  [
   {handlers,
    [
     {lager_console_backend,
      [
       {level, info}
      ]},
     {lager_syslog_backend, ["erlang", local1, info]}
    ]}
  ]}
].
